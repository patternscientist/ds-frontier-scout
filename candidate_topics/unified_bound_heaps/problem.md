# Problem

Design a pointer-model priority queue with constant-time decrease-key and a working-set-style extract-min bound, where extracting an item is cheap if few operations occurred while that item was present.

Open status: open.
