# Why It Matters

Karp-Rabin fingerprints are a core randomized tool in string algorithms and string data structures. Fast collision detection would make fingerprint-based methods more certifiable and robust.
