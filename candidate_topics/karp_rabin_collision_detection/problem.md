# Problem

Given a string and a prime modulus used for Karp-Rabin fingerprints, decide whether any two distinct equal-length substrings collide under that modulus faster than the straightforward quadratic method.

Open status: open.
