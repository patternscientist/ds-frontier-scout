# Problem

Determine the true amortized complexity of the standard pairing heap, especially the decrease-key operation, or close the gap between known lower and upper bounds.

Open status: open.

This should focus on the standard two-pass pairing heap unless a variant is explicitly marked as such.
