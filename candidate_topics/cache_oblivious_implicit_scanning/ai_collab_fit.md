# AI Collaboration Fit

Good for layout experiments, invariant extraction, and separating model variants.

Promising as a theorem project if the next pass confirms the scan problem is still open under the strict implicit model.
