# Blind Prompt

In the external-memory model with block size B and memory M, design or lower-bound a priority queue supporting Insert, ExtractMin, and DecreaseKey. Focus on matching known lower bounds or identifying a clean work/I/O tradeoff where an algorithm is simultaneously near-optimal in RAM work and I/O complexity.
