# Blind Prompt

Define strong history independence for a memory allocator managing variable-size records. For each abstract set of live records and sizes, the observable memory-layout distribution must be independent of the operation history.

Design an allocator with low fragmentation and polylogarithmic or expected constant operation overhead, or prove a lower bound in a simple RAM/block model.
