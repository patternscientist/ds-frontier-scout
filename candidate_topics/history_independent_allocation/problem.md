# Problem

Design a history-independent allocator for variable-size records with low time and space overhead.

The memory representation should reveal only the current set of live records and sizes, not the allocation/deallocation sequence that produced them.

Open status: open in Naor-Teague 2001; modern status uncertain.
