# Frontier

Batch 002 source says polylogarithmic update time is unknown even for a key min-tree-cut subroutine. Recent work reaches subpolynomial dynamic min-cut guarantees, so the exact subroutine frontier must be rechecked.
