# Frontier

Batch 002 working frontier:

- Dietz gives early `O(log log m)`-type persistent-array bounds with expected amortized updates.
- Straka improves worst-case persistent arrays.
- The suspected remaining gap is worst-case `O(log log m)` for both operations with `O(n+m)` space.

TODO: verify exact statements and lower bounds from the primary text.
