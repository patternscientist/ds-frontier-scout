# Problem

Maintain a fully persistent array with point lookup and point update over `m` updates and initial length `n`, using `O(n+m)` space and worst-case `O(log log m)` time per operation.

Open status: uncertain. Batch 002 found secondary and follow-up sources indicating this exact linear-space worst-case tradeoff remains open, but the thesis/paper text must be checked directly before promotion.
