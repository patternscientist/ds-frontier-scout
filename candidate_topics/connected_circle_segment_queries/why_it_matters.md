# Why It Matters

Circle/segment queries on connected graphs model road networks, map matching, robot collision checks, and mesh-like geometric inputs.

The niche theoretical point is clean: connectivity changes the data-structure landscape compared with arbitrary segment sets.
