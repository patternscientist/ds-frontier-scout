# Problem

In the imprecise-comparison model, comparisons are guaranteed correct only when item values differ by at least a fixed threshold. Determine whether randomized maximum finding with error 2 can be done using `O(n)` comparisons, and clarify randomized sorting with error `k`.

Open status: open in Ajtai-Feldman-Hassidim-Nelson; modern status uncertain.
