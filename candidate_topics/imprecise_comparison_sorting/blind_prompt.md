# Blind Prompt

There are `n` unknown real-valued items. A comparison between two items is guaranteed correct only if their true values differ by at least 1; otherwise either answer may be returned adversarially.

Give a randomized algorithm using `O(n)` comparisons that returns an item within value 2 of the maximum with high probability, or prove a lower bound. Also consider randomized sorting with error `k`.
