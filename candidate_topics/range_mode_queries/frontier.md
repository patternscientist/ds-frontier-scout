# Frontier

Known from Batch 002:

- Linear-space exact range mode has `O(sqrt(n))`-style query upper bounds.
- Cell-probe lower bounds are far below the upper bound.
- Range alpha-majority has adjacent open questions on space and output-sensitive reporting.

TODO: verify the strongest current static exact range-mode upper/lower bounds after 2012.
