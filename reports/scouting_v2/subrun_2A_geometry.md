# Subrun 2A: Geometry Source Pool

Status: not started.

Role: search computational geometry and geometric data-structure sources for narrow, under-attended open problems. This subrun must remain inside its assigned source pool and must not use broad cross-area searching to promote candidates.

## Assigned Source Pool

Allowed sources:

- Computational geometry conference proceedings and problem sessions, especially SoCG, ISAAC geometry tracks, CCCG, EuroCG abstracts, and Dagstuhl geometry seminars.
- Primary papers, preprints, and author pages on geometric data structures, kinetic data structures, range searching, geometric lower bounds, geometric graph queries, and external-memory geometry.
- Geometry-focused lecture notes by domain experts when they state or curate open problems.

Out of scope:

- General graph algorithms without a geometric data-structure component.
- Broad algorithmic lower-bound surveys unless they point to a geometry-specific primary source.
- Existing candidate folders unless used only for duplicate checking or recording updates.

## Required Checks Before Promotion

- Check `candidate_topics/` slugs and `reports/top_20_shortlist.md`.
- Separate duplicates from genuinely new geometry leads.
- Run a last-24-month freshness check for every candidate.
- Record exact source URLs, DOI/arXiv IDs, titles, search terms, and date searched.
- Mark `open_status` as `explicit`, `inferred`, `uncertain`, or `likely_solved`.
- Do not promote more than five candidates; fewer is acceptable.

## Candidate Findings

No subrun performed yet.

## Rejection Ledger

No subrun performed yet.

## Notes For Synthesis

When this subrun is complete, summarize only source-supported claims here. Distinguish explicit source statements from inferred gaps and scouting hypotheses.

